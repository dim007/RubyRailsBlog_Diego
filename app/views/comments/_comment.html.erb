<%-# _comment is a view a 'partial', refactored code from show.html.erb-%>

<%= if comment.commenter && comment.body then react_component("CommentCard", title: comment.commenter , body: comment.body, article_id: comment.article.id , comment_id: comment.id) end %>
<br/>